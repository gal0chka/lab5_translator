# README.md

## Автор
Галкин Артемий  
КМБО-02-23  
Вариант: 6

## Возможности программы
* **Арифметические операции:**
  * `+` (сложение)
  * `*` (умножение)
* **Функция возведения в степень:**
  * `pow(a, b)`
* Поддержка **положительных целых чисел**
* Поддержка **вложенных выражений** (например: `pow(2, pow(3, 4))`)

## Грамматика

<Expr> ::= <Sum>
<Sum> ::= <Prod> <SumTail>
<SumTail> ::= "+" <Prod> <SumTail> | ε
<Prod> ::= <Atom> <ProdTail>
<ProdTail> ::= "*" <Atom> <ProdTail> | ε
<Atom> ::= INT | "pow" "(" <Expr> "," <Expr> ")"

## Сборка и запуск
Программа написана на **Python 3**

### Запуск с файлом:
python translator.py выражение.txt

### Итеративный режим:
python translator.py
4+5*6
^Z